export declare enum AutoMoveType {
    NOT_READY = "not ready",
    STOP = "stop",
    MOVE = "move",
    PAUSE = "pause",
    ERROR = "error",
    VIR = "vir"
}
export declare enum PathType {
    NONE = "none",
    REQ_PATH = "req_path",
    RECV_PATH = "recv_path"
}
export declare enum NodeState {
    NONE = "none",
    MOVE = "move",
    COMPLETE = "complete",
    FAIL = "fail",
    OBSTACLE = "obstacle",
    CANCEL = "cancel"
}
